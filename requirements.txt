# Contribution Factor Model v3.0 - Requirements
# Python 3.8+

# Data validation
pydantic>=2.0.0          # Data validation using Python type hints (required by anthropic)

# Core dependencies
anthropic>=0.25.0        # Anthropic Claude API (for Cycle 3 LLM evaluation)
python-gitlab>=3.15.0    # GitLab API client (for repository data collection)
gitpython>=3.1.40        # Git repository operations (for blame analysis)
tenacity>=8.2.0          # Retry logic with exponential backoff (for API resilience)
python-dotenv>=1.0.0     # Environment variables from .env (for credential management)

# Anthropic dependencies (explicit to avoid version conflicts)
httpx>=0.24.0            # HTTP client for Anthropic API calls
anyio>=4.0.0             # Async I/O library
sniffio>=1.3.0           # Async framework detection
jiter>=0.2.0             # Fast JSON parsing
distro>=1.9.0            # Linux distribution identification
docstring-parser>=0.15   # Docstring parsing for API schemas

# Testing
pytest>=8.0.0            # Testing framework
pytest-cov>=4.0.0        # Coverage plugin for pytest

# Development
black>=23.0.0            # Code formatter
flake8>=6.0.0            # Linter
mypy>=1.0.0              # Type checker
